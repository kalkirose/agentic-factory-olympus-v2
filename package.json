{
  "name": "olympus-v2",
  "version": "0.0.1",
  "description": "Autonomous development harness: durable orchestrator daemon, ledger-first runs, agent seats for judgment only.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "olympusd": "bin/olympusd.mjs",
    "olympusctl": "bin/olympusctl.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs"
  }
}
